# @skill-harness/adapters

Harness adapters for [skill-harness](https://github.com/mojomanyana/skill-harness):
the `pi` runner + `claude-code` judge routing.

Internal — use the `skill-harness` CLI. See the repo for docs.

ESM, Node ≥ 20.
