# Connected local evidence replay — bounded P16 slice

The software/layout daily worlds, role labels and operator decisions are **synthetic fixtures**. The archive, case decision, investigation, trust and blind-review paths are actual implementations. An explicit optional producer pin adds actual fixed-profile P15 workers and reserved decisions. Neither mode is a live daily case, model experiment, authenticated human review or full-factory qualification.

After direct build, choose a NEW owned output directory under an existing private parent:

```sh
node examples/factory-local-replay/replay.mjs --output /absolute/private/new-replay
```

This default performs no worker/model dispatch. It leaves archive/spec/candidate/summary evidence for inspection and edits only its newly created fixture candidate. Never point it at historical evidence. No install or publication occurs.

## Actual connected harness path

1. Pinned P01 builders create the synthetic two-obligation world with three declared fixture attempts. Only scope/intent/obligation revisions declare the fixed read effect; policy/artifact revisions retain their required empty effects. Independent fixture authority is reconstructed from the expected world, not incoming claims. Session/branch labels are not native authentication.
2. Actual archive/projection and structural nomination run without a manually captured note. A fixture operator confirms a defect in the real selected-batch decision store.
3. The durable weekly supervisor reads that exact decision, freezes one weekly selection and runs its actual bounded data-only inert interpreter. Only approved archive reads and the linked hypothesis are supported; there is no shell/network/model dispatcher.
4. Separate investigation and exact preview permissions drive the existing spec writer. Actual spec/rubric/judge/heldout/configuration bytes freeze before an explicitly permitted edit of the new candidate file. Reopen validates the freeze; a copied store cannot claim the same week.
5. Durable predeclared trust reads the same actual case decision, joins an explicit prediction to its retained reference and recomputes calibration. Its one confirmed fixture outcome is labelled **synthetic-confirmation**, not measured detector reliability. Unit tests separately cover8/10 plus3 unresolved, unflagged misses, corrections, stale confirmations and attention/refill denial.
6. Real retained blind records keep quality before reveal across reconnect. Roles, objective labels and costs in this portion remain synthetic, including the cheaper failing arm. No routing default is produced. Hypothetical adoption authorization remains a fixture predicate, not applied production activation; later outcome stays unknown.

## Opt-in actual P15 execution

Use a separate, source-clean checkout of the exact commit/tree in `producer-order-pin.json`. It pins corrected producerf276698 and its contract/controller bytes, not a moving branch. Supply existing dependencies at both repository and package levels, without installing or modifying shared trees. Prefer a dedicated immutable copy, not an active writer's checkout. This source-loaded route needs a retained Node runtime supporting the producer's TypeScript entry points (locally exercised on26.7), Linux, existing `/usr/bin/bwrap` and `/usr/bin/prlimit`, and actual namespace support. Missing prerequisites fail; no fallback or guard relaxation.

```sh
node examples/factory-local-replay/replay.mjs \
  --output /absolute/private/new-connected-replay \
  --producer /absolute/source-clean/pinned-producer
```

The optional route verifies HEAD/tree, tracked source and pinned contract hashes before loading the **actual producer controller**. It uses the same work scope and the durable investigation's actual frozen/candidate digests as worker input. Each domain reserves two attempts upfront, stops at the real reserved decision, applies an explicitly labelled fixture decision, then drains the original controller. Software satisfies both nodes. Layout deliberately mismatches its second fixed requirement and exhausts that node. Both charge two attempts and finish with zero active reservations; artifacts are read through the real controller. The applied-adoption phase then charges **two additional fixed attempts per domain** (four total per domain), under a separate explicit budget. No model call or live acceptance event is fabricated.

`actual-order.json` records exact producer identity, investigation/case linkage, policy pin, actual states, resource accounting and artifact hashes. `harness-phase-summary.json` preserves the earlier synthetic phase. Final `summary.json` becomes `inert-connected-replay`, still `liveQualified:false` and `acceptance:not-assessed`.

Retained development failures include the initially missing obligation read effect, then wrongly assigning an effect to a policy/artifact, and an incomplete private-copy dependency map (`typebox` unavailable). These were fixture/copy repairs, not relaxed producer guards. The ordinary unit suite does not silently skip required namespace tests: this optional external integration is separately executed and reported; default tests exercise the harness path without claiming native P15 coverage.

## Applied policy and Principal invocation

The optional native route now applies a changed fixed-policy suffix through the actual registry. Missing exact activation authority is refused without changing the revision; replaying the authorized activation is idempotent. The original order remains revision0. A real next order pins revision1 and the adoption receipt, executes with the changed suffix, and retains its artifact. Software succeeds; layout still exhausts its second requirement. `applied-adoption.json` links the actual observed bytes to that adoption. Eligibility/authority remain **synthetic declarations**; the production observation is **unknown**, not confirmed success/escape or automatic rollback.

Optionally append `--principal /absolute/source-clean/pinned-principal` to the producer command. `principal-pin.json` pins abf3525 and the actual association/reference/AssuranceStore source. Both checkouts are verified before creating output. The replay invokes the actual P04 daily reader on the same retained work/context and the actual Principal native reference producer/v3 consumer. A newly created, explicitly synthetic native ledger records the real retained-order predicate (software exit0, layout exit1), exact native receipt bytes and separate native candidate/context identities. No campaign approval/finalization command runs.

The v3 link must be current, reject a wrong artifact digest, and retain its original bytes after explicit native-reference retirement. `principal-fixture/` retains input/write/current/error/retired readbacks. Generic retirement remains **unbound** until the separately assigned generic lifecycle connector supplies it; receipt bytes are not full check stdout. Principal source identity is not semantic equivalence to the generic artifact or candidate. This actual invocation is still `liveQualified:false` and `acceptance:not-assessed`.

Remaining A work: generic lifecycle/payload/host assembly connector (same Principal owner), downstream consumption, and SPEC004 role/configuration/panel/casting orchestration. B/C: approved live charter, actual delivery/role/effect qualification, authenticated debrief/presentation, real daily/model experiments, second-domain skill qualification and later production outcomes. Original whole-change CHANGES-REQUESTED remains preserved.
