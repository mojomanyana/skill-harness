# Software factory — current delivery state

Updated 2026-09-12. Dated build reports and ADRs remain historical evidence.

## Release identities

| Component | live package baseline | merged source | next release |
|---|---|---|---|
| skill-harness | 0.14.0 | `e00941b0565e75c9380270a1f3a536b163ecc159`, preserving reviewed `6b34439` | 0.15.0 release preparation |
| pi-daddy | 0.24.0 | `5cea7c03f926c50607bc8a16f320040b80dfb8f0`, preserving reviewed `8b8fc7d` | 0.25.0 release preparation |
| principal-pi-skills | 3.2.0 | unchanged | no release |

Merged source, canonical release artifacts, registry bytes, installed package and loaded-session identity remain separate facts. Follow `PUBLISHING.md`; do not treat a candidate or smoke result as publication.

## Current capabilities and boundaries

- Declared work and ordinary occurrence accounting remain separate from acceptance.
- Daily steering exposes labelled priority, recorded scope-successor and same-scope alternative actions through exact intent authority/CAS. Scope changes rebind persisted and current-session declarations before ordinary dispatch resumes.
- Closing review requires explicit presence plus paused, reservation-free and ordinary-quiescent state. Missing trust qualification spends zero of five cards.
- Opt-in measured Sol/Terra sessions retain observed identity and Pi Usage under no-tool bounds. Catalog cost is not subscription billing, and ordinary print-child Usage remains unavailable.
- Bounded measured orders support deterministic exact/free-form qualification, actual sealed predecessor output, original-owner unknown accounting and acknowledged declared recovery.
- Projection-only runtime facts and monotonic learning lifecycle navigation are retained. Human quality choice, trust calibration, configured adoption, publication outcome and later outcomes remain independent evidence.
- The public NWS comparison is genuine second-domain evidence. It is not the failure-derived abort-race improvement chain.

## Release evidence

PR #78 and PR #41 were independently reviewed, repaired red-first, merged with their exact candidate ancestry, and passed exact-head CI. Release/version PRs, canonical Node 20.20.2/npm 10.8.2 packaging, fresh bounded path smoke, registry publication and downloaded-byte verification remain ordered release steps.

The previous real C08 SDK comparison runs are retained real no-tool subscription occurrences. Offline validation of those bytes against the measured-order contract is a no-model replay, not a new packaged-route occurrence. A release smoke proves paths only, not efficacy or human preference.

## Exact unresolved human actions

1. Supply independently labelled `flagged`/`unflagged` calibration manifests for detector `coverage_gap`, version `work-signals-v1:declared-work-v1`, and exact `daily:<host-id>` population before acknowledging closing cards.
2. Choose one of the rendered abort-repair variants, then separately authorize or reject configured adoption for future orders.
3. Choose one of the rendered public-bulletin variants; reveal, publication outcome and later outcomes stay separate.

No absent input is interpreted as acceptance.
