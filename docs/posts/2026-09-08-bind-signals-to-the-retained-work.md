# Draft: bind signals to the retained work

The work-signal path now has an adapter from the actual pinned Work-v4 reader. Scope, revision bindings, acceptance and coverage come from that projection, not replacement fields supplied alongside a proposed warning. The source, projection, frozen input and review batch stay linked in the private archive.

Missing authority remains coverage, not a defect. This still requires independently established checkpoint/outcome facts and host authority; it does not authenticate live work or silently reinterpret artifact content digests as revision identities.
