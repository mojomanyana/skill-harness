# Draft: connected is not qualified

The two-domain replay now connects actual confirmed-case decisions to the durable investigation and trust lifecycle. Promotion uses the existing spec writer, evaluation files freeze before an approved fixture-candidate edit, and retained screens use the existing delivery-aware validator. Old results without delivery evidence remain UNKNOWN; linked screens remain exploratory.

An explicit producer pin can also run the corrected P15 controller: two real fixed-profile attempts per domain, an actual reserved decision, original-controller settlement and retained artifact hashes. Software satisfies both fixed requirements; layout intentionally exhausts one. Both settle with zero active reservations.

These are real implementations and real fixed workers, under **synthetic fixture authority**. No model runs, authenticated human decisions or live campaign acceptance are invented. The corrected Principal mapping still needs its own settled pin; model qualification and later production outcomes remain separate.

The initial missing-effect, invalid policy/artifact-effect and incomplete private dependency-map refusals stay in the evidence. The fixtures changed—not the guards.
