# Draft: project the retained work, not a display summary

The archive now reuses the pinned producer's actual pure work-v4 reader/projector. Its generated facade has no append or filesystem-inspection API. Raw retained work, derived facts and silent case batches stay linked.

The producer fixture preserves two attempts and three variants; missing host authority does not become accepted work. Redacted/missing sources and malformed tails cannot be silently promoted into a valid work projection. Live observation and authenticated authority remain separate integration requirements.
