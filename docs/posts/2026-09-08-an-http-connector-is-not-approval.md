# Draft: An HTTP connector is not execution approval

The prepared Codex entry now connects the existing journal and SDK wire validator to a fixed native HTTPS port. A separate expiring approval binds the frozen charter and one journal path; unknown canonical resolution, changed policy and fixture/live port mismatches stop before credential or transport effects.

OAuth is read through a lazy bounded port, with no API-key/helper/refresh fallback. The real credential never reaches SDK hooks or subject frames. Client calls, byte reservations, time and one in-flight request are bounded; provider generation and token limits are not invented.

Network-free tests exercise the installed SDK with explicitly fake ports. Native HTTPS tests mock I/O, not a TLS success claim. A prepared connector is not a live result, host resource qualification, native acceptance or publication permission. The private five-call manifest remains unexecuted and its unresolved evidence stays visible.
