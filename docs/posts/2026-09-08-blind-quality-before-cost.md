# Draft: quality before cost, without changing fixed-model comparisons

A separate frozen intervention family declares model/effort and other changed axes while preserving the existing fixed-model comparison guard. Its read-only assessment requires independent host role/evidence context and actual retained output bytes; missing delivery or cheap failures cannot become eligible winners.

The blind view exposes opaque labels and retained artifacts before a choice, then permits cost/configuration reveal and locks quality changes. No routing or adoption default follows. Host persistence, real identity/route qualification and live casting remain separate pending work; the shipped UI fixtures are explicitly synthetic.
