# Draft: invoke before claiming a link

A source pin is not a working connector. The replay now invokes the pinned Principal producer and v3 reader against an actual P04 read of its retained work. It keeps current evidence, rejects a mismatched artifact reference, and retains failed evidence after explicit retirement. The native ledger is a clearly labelled fixture, not campaign acceptance.

Adoption also crosses its former predicate-only boundary: a real registry activation changes the next order's policy without repinning the original order. Two more fixed attempts per domain exercise the new policy. The observed artifact belongs to that adoption, but the production outcome remains unknown: fixed worker success and synthetic eligibility are not independent production confirmation.

Generic lifecycle/full-payload connectors and live authority remain separate work. Neither this implementation nor its tests replace the original CHANGES-REQUESTED verdict.
