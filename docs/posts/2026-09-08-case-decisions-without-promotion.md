# Draft: a case decision is not a promotion

Selected-batch work review now persists explicit operator decisions in a private append-only history. Stale updates, altered history, another writer's lock and incomplete tails fail closed; exact replay cannot overwrite a later decision. Bounded pages leave unselected cards unresolved.

This storage does not authenticate a human, trigger a worker, expose alerts or promote a scored scenario. Those host/presentation policies remain separate. A real two-process fixture admitted one initial decision, and contract fixtures preserve explicit skip rather than turning it into agreement.
