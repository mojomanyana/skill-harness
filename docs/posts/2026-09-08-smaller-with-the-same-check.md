# Draft: smaller, with the same check

One touched-area maintenance pilot removed a single-use frozen-manifest reconstruction helper. The full comparison remains, old tests and fixtures are unchanged, and the rebuilt extension bundle is byte-identical. It removes one function/allocation and83 source bytes—not a validation obligation.

This is local deterministic maintenance evidence, not a model benchmark or a claim that the whole factory is qualified.
