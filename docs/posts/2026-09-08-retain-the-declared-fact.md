# Draft: retain the declared fact, not just its warning

The additional work-signal rules now have a private, closed input archive. Reads verify the retained bytes and derive nominations again instead of accepting cached verdicts. Unknown fields, conflicting obligation identities and missing bytes fail visibly; malformed JSON cannot leak raw excerpts through diagnostics.

These are retained host declarations, not authenticated observations or proof that every referenced artifact exists. Case-v3 presentation and live fact sourcing remain separate integration work. Old case-v2 readers are not silently widened.
